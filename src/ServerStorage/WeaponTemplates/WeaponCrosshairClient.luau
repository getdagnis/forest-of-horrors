-- Legacy Tool-local crosshair owner intentionally disabled.
-- GunClientController.client.luau owns the shared HUD, crosshair, and cursor
-- state for every supported weapon. Keeping this script inert prevents a
-- second HUD and a competing MouseIconEnabled restore on unequip.
