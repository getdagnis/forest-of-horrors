-- Retired deliberately. MonsterKill remains an audio-only reward trigger:
-- its +5 is added to the regular score HUD by MonsterSystem. Keep the Studio
-- MultikillHud LocalScript disabled; this no-op prevents a future accidental
-- enable from restoring any duplicate kill-chain announcement.
